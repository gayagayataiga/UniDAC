[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "unidac"
version = "0.0.0"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["unidac*"]
